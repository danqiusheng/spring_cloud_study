spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
          git:
            uri: https://github.com/danqiusheng/spring_cloud_study/
           # username:
           # password:
            search-paths: spring_cloud_config/config-repo
server:
  port: 7001

