spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:7001/
server:
  port: 5556
eureka:
  client:
    service-url:
          defaultZone: http://localhost:1111/eureka/

management:
  security:
    enabled: false